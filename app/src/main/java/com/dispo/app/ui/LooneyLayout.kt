package com.dispo.app.ui

/** Diamètre du bouton vert = cette fraction de la plus petite dimension écran. */
const val DISPO_BUTTON_FRACTION = 0.52f

/** Cœur jaune/orange de la tornade (même repère que [LooneyRings]). */
const val LOONEY_CORE_OUTER_FRACTION = 0.36f
const val LOONEY_CORE_YELLOW_FRACTION = 0.325f
